apiVersion: v1
kind: Secret
metadata:
name: {{ .Values.secret.name }}
namespace: {{ .Values.namespace }}
type: Opaque
data:
MONGO_INITDB_ROOT_USERNAME: {{ .Values.secret.username | b64enc }}
MONGO_INITDB_ROOT_PASSWORD: {{ .Values.secret.password | b64enc }}
