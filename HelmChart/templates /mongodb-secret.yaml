apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secret.name }}
type: Opaque
stringData:
  MONGO_INITDB_ROOT_USERNAME: {{ .Values.secret.username }}
  MONGO_INITDB_ROOT_PASSWORD: {{ .Values.secret.password }}
