apiVersion: v1
kind: Service
metadata:
  name: candidate-service
  labels:
    app: candidate-service
spec:
  type: ClusterIP
  selector:
    app: candidate-service
  ports:
    - port: 80
      targetPort: 3000
